# Publishing House — Project Spec
# Lifecycle: intake → content_review → infra_review → jira_sync → development → ready → published
# No secrets in this file. Tokens live in ~/.config/publishing-house/auth.json
# Stage is managed by SonataFlow and cached in .ph-state — do not edit stage.current manually.
# NOTE: This file is also in the template repo. The Scaffolder overlays project.* values.
# TODO: Eliminate this copy — Scaffolder should pull from template repo directly.

project:
  slug: "tyrell-test2"
  owner_email: "treddy@redhat.com"
  content_type: "lab"
  deployment_mode: "rhdp_published"
  initiative_key: "rh1_2027"
  showroom_type: "classic"
  intake_type: "new"
  automation_type: "ansible"
  description: "A hands on lab for installing keycloak on openshift 4.21. One module lab for 30 mins for tech sales staff with intermediate experience."
  # content_reviewer_email and infra_reviewer_email are assigned by the RHDP team — not set by the author
  jira_ticket: "RHDPCD-1314"
  workflow_id: "fa14e414-a5ae-4a43-a54d-9cd1be3864c0"

spec:
  title: "Installing the Keycloak Operator on OpenShift"
  learning_objectives:
    - "Install the Red Hat build of Keycloak operator from OperatorHub on OpenShift 4.21"
    - "Verify a successful operator deployment via the OpenShift web console"
  modules:
    - id: module-01
      title: "Installing the Keycloak Operator"
      duration_min: 30
      status: not_started
  # [{id, title, duration_min}] — written by intake Phase 4; id is stable (e.g. module-01)
  duration_hours: 0.5
  audience: "intermediate"
  environment:
    platform: ""               # ocp | rhel-vms — determines which sizing fields apply
    topology: ""               # shared-cluster | per-student | cnv-pool
    cloud_provider: cnv        # cnv | aws | troshka — cnv is default
    max_concurrent_users: null # required if topology = shared-cluster

    # --- OCP-specific fields (only when platform = ocp) ---
    ocp_version: ""                    # e.g. 4.20 — minimum 4.20; leave empty for rhel-vms
    cluster_type: ""                   # sno | multinode — only for platform = ocp
    control_plane_instance_count: null # 1 if sno, 3 if multinode
    control_plane_cpu: null            # 32 if sno, 16 if multinode
    control_plane_ram_gb: null         # 128 if sno, 64 if multinode
    worker_count: null                 # number of OCP worker nodes
    worker_cpu: null                   # vCPU per worker
    worker_ram_gb: null                # RAM per worker in GB
    worker_disk_gb: null               # disk per worker in GB

    # --- VM-specific fields (only when platform = rhel-vms) ---
    # Each entry: {role, count, cpu, ram_gb, disk_gb, os}
    vms_per_student: []

    # --- Common fields (both platforms) ---
    aap_version: ""                    # required if AAP in products
    ai_requirement: ""                 # maas | gpu | none
    ai_model_tier: ""                  # open-source | frontier (if ai_requirement = maas or gpu)
    ai_model_name: ""                  # specific model e.g. granite-3.1-8b
    ai_justification: ""               # required if ai_model_tier = frontier or ai_requirement = gpu
    non_ga_products: []                # list of non-GA products/versions
    non_ga_access_plan: ""             # how access is provided for non-GA products
    external_services: []              # named external service dependencies e.g. [github.com]
    gpu_nodes: 0                       # number of GPU nodes (0 = none)
    gpu_type: ""                       # e.g. NVIDIA A100 40GB (required if gpu_nodes > 0)

development:
  automation:
    ansible:
      status: not_started
  e2e:
    status: not_started
  healthCheck:
    status: not_started

compliance:
  last_hard_check: null
  hard_result: null

tracking:
  paused: false
  pause_reason: null

# Approval checklist — filled collaboratively by author + skill + system + reviewers
approval_checklist:

  content:
    prerequisites_verifiable: false
    assessment_strategy: ""
    catalog_gap: ""
    design_overview: ""
    module_summaries: []
    rcars_overlap_pct: 43
    rcars_top_matches:
      - title: "OpenShift Ops Day Roadshow"
        ci_name: "published.openshift-days-ops-track.prod"
        url: "https://catalog.demo.redhat.com/catalog?item=published.openshift-days-ops-track.prod"
        relevance_score: 43
        why_it_fits: "Includes a dedicated 'OpenID Connect Authentication with Keycloak' module covering Keycloak realm and client configuration as an OIDC identity provider on OpenShift."
        workloads:
          - role: "ocp4_workload_cert_manager"
            collection: "agnosticd.core_workloads"
          - role: "ocp4_workload_external_odf"
            collection: "agnosticd.core_workloads"
      - title: "LB1464: Unifying DevSecOps: Secure, Standardized Application Delivery Across Containers (CNV)"
        ci_name: "summit-2026.lb1464-unified-devsecops-delivery-cnv.prod"
        url: "https://catalog.demo.redhat.com/catalog?item=summit-2026.lb1464-unified-devsecops-delivery-cnv.prod"
        relevance_score: 36
        why_it_fits: "Includes Red Hat build of Keycloak as a supporting OIDC identity integration component, but focus is on DevSecOps supply chain security — not Keycloak operator installation."
        workloads:
          - role: "ocp4_workload_authentication_keycloak"
            collection: "agnosticd.core_workloads"
      - title: "Platform Engineering Roadshow"
        ci_name: "published.platform-engineering-workshop.prod"
        url: "https://catalog.demo.redhat.com/catalog?item=published.platform-engineering-workshop.prod"
        relevance_score: 35
        why_it_fits: "Includes Red Hat build of Keycloak as an identity provider for Developer Hub, but targets platform engineers and does not teach Keycloak operator installation from OperatorHub."
        workloads:
          - role: "ocp4_workload_platform_engineering_workshop"
            collection: "rhdp.workshop"
    catalog_gap: "No existing catalog item teaches installing the Keycloak operator from OperatorHub as a standalone objective for tech sales staff. Existing Keycloak content appears as a secondary component in longer, advanced platform engineering workshops — not as a focused 30-minute lab for an intermediate sales audience."
    rejections: []

  infra:
    peak_environments: null
    cost_per_run_est: ""
    provisioning_time_est: ""
    agnosticv_base_ci: ""
    approved_by: ""
    rejections: []
